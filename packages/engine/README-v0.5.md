# Spatial Engine v0.5 — Layout Engine

Adds deterministic value-to-position mapping with Grid and Circle layouts.

## Merge and test

Copy `packages/engine` into the repository root, then run:

```powershell
cd packages/engine
npm install
npm test
npm run typecheck
npm run build
```

No database migration is required.
